#include<stdio.h>
#include<string.h>
int main(){
	char frase[100]	;
	int palavras=1,i;
	fgets(frase,100,stdin);
	for(i=0; i<strlen(frase); i++){
		switch (frase[i])
		case 'a': 
	}
	
	printf("voce escreveu %d palavras",palavras);
	
	return 0;
}
